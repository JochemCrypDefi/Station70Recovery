# The whole tool: everything needed to decrypt keys, produce wallet-extension
# import strings, and wrap a key for AWS KMS import.
#
# Nothing here opens a network connection. `cryptography` covers RSA-OAEP and
# the RFC 5649 AES key wrap that KMS import requires, so no AWS SDK is pulled
# in and the offline guarantee is a property of the dependency list itself.
#
# Offline install: on an internet-connected machine run
#     pip download -r requirements.txt -d wheels --platform win_amd64 \
#         --python-version 312 --only-binary :all:
# copy the `wheels` directory across, then on the air-gapped machine
#     pip install --no-index --find-links wheels -r requirements.txt

cryptography>=42.0
pycryptodome>=3.20
pynacl>=1.5
textual>=0.79
rich>=13.7
